<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Teaddy</title>
  <link rel="stylesheet" href="/src/css/main.css">
  <% if(page === 'login') { %><link rel="stylesheet" href="/src/css/login.css"><% } %>
  <% if(page === 'register') { %>
  <link rel="stylesheet" href="/src/css/step.css">
  <% } %>
</head>
<body>
  <!-- header -->
  <nav>
    <ul id="topbar">
      <li>
        <a href="/" class="topbar-item"><h3>Teaddy</h3></a>
      </li>
      <li id="topbar_account">
        <span id="topbar_account_item" class="topbar-item nav-dropdown-keep-show">계정</span>
        <div id="topbar_account_content" class="nav-dropdown-content nav-dropdown-keep-show">
          <% if(!user) { %>
          <a href="./login" id="topbar_account_login" class="card">
            <span>다양한 기능을 이용하시려면 <b><u>로그인</u></b>하세요.</span>
          </a>
          <% } else { %>
          <div class="card">
            <span><%= user %></span>
          </div>
          <% } %>
        </div>
      </li>
    </ul>
  </nav>
